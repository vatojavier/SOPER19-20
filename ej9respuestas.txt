a)El proceso padre no imprime nada, no es correcto porque al la memoria reservada por el padre no es copartida con el hijo es decir, se duplica, haciendo así que el mensaje del padre esté vacío.

b)Hay que liberar memoria en ambos porque al no ser mem. compartida, también se crea esa memoria para todos los hijos.

/*...*/
else if (pid == 0)
{
	strcpy(sentence, MESSAGE);
	free(sentence);
	exit(EXIT_SUCCESS);
}
else
{
	wait(NULL);
	printf("Padre: %s\n", sentence);
	free(sentence);
	exit(EXIT_SUCCESS);
}
/*...*/
